{
  "name": "@adama/tailwind-config",
  "version": "1.0.0",
  "private": true,
  "main": "tailwind.config.js",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules",
    "update-dependencies": "pnpx npm-check-updates@latest -u"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}